<p style="color: green"><%= notice %></p>

<h1>All Lists</h1>

<div id="lists">
  <% @lists.each do |list| %>
    <%# <%= render partial: 'list', locals: { list: list } %>
    <div class="list card-category"
      style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path(list.photo.key, height: 300, crop: :fill) if list.photo.present? %>')"
    >
      <%# <%= cl_image_tag @list.photo.key, height: 300, width: 400, crop: :fill %>
      <h2><%= list.name %></h2>
      <!-- Add any other details you want to display for each list -->
    </div>

    <p>
      <%= link_to "Show this list", list %>
    </p>
  <% end %>
</div>

<%= link_to "New list", new_list_path %>
